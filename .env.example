# Flight Planner Environment Configuration
# Copy to .env and fill in values

# ============================================================================
# SECURITY NOTICE
# ============================================================================
# DO NOT put API keys in .env files!
# All external API keys are stored as Cloudflare Worker secrets.
# See SECRETS.md for configuration instructions.
# ============================================================================

# Cloudflare Worker URL (after deploying)
VITE_WORKER_URL=flight-planner.flight-planner.workers.dev

# The following keys are NO LONGER used in the frontend.
# They are configured as Cloudflare Worker secrets instead:
# - ICAO_API_KEY → wrangler secret put ICAO_API_KEY
# - OPENAIP_CLIENT_ID → wrangler secret put OPENAIP_CLIENT_ID  
# - WEATHER_API_KEY → wrangler secret put WEATHER_API_KEY
